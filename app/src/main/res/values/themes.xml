<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WifiChat" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSurfaceContainer">?attr/colorSurface</item>

        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:statusBarColor">?attr/colorOnSurfaceInverse</item>
        <item name="android:navigationBarColor">?attr/colorPrimaryVariant</item>
        <item name="actionBarStyle">@style/CustomActionBar</item>
        <item name="fontFamily">monospace</item>
    </style>
    <style name="CustomActionBar" parent="@style/Widget.Material3.ActionBar.Solid">
        <item name="titleTextStyle">@style/CustomActionBarTitle</item>
    </style>

    <style name="CustomActionBarTitle" parent="@style/TextAppearance.Material3.ActionBar.Title">
        <item name="android:fontFamily">monospace</item>
        <item name="fontFamily">monospace</item>

    </style>
</resources>